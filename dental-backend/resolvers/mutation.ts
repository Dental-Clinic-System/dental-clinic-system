const mongoose = require("mongoose");

const mutations = {
    async addBook(_: any, params: any, context: any) {
        // const user = checkAuth(context)
        return "book"
    },
}

export default mutations;