name: frontend-build
on: push
jobs:
  deploy:
    runs-on: macos-latest
    env:
      workingDirectory: dental-front
    steps:
      - uses: actions/checkout@master

      - name: installing modules
        run: yarn && yarn add firebase-tools
        working-directory: ${{env.workingDirectory}}

      - name: building ...
        run: yarn build
        working-directory: ${{env.workingDirectory}}

      - name: deploying ...
        run: yarn deploy --token "1//0e1BZH0Sff2ABCgYIARAAGA4SNwF-L9Irw0QPpTwQYQcQ9NICqmtl_91qNxND8bECT_lFIQtWBt1ED2rbFkuMTZ-s3Q79snbemwo"
        working-directory: ${{env.workingDirectory}}